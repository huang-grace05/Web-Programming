# Cherry-Pick

`2fd8895` on `lab09-feature` and `d13c8af` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.